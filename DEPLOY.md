# 自动部署指南

本项目已配置三种自动部署方案，您可以根据需求选择其中一种：

## 🚀 部署方案

### 1. GitHub Actions + GitHub Pages (推荐)

**特点：** 完全免费，与 GitHub 无缝集成

**设置步骤：**
1. 在 GitHub 仓库设置中启用 GitHub Pages
   - 进入 Settings → Pages
   - Source 选择 "GitHub Actions"
2. 设置环境变量：
   - 进入 Settings → Secrets and variables → Actions
   - 添加 `GEMINI_API_KEY` 密钥
3. 推送代码到 `main` 分支即可自动部署

**访问地址：** `https://[username].github.io/seek-stars/`

---

### 2. Vercel (推荐)

**特点：** 部署速度快，自动 HTTPS，支持预览部署

**设置步骤：**
1. 访问 [vercel.com](https://vercel.com) 并用 GitHub 账号登录
2. 导入 `seek-stars` 仓库
3. 设置环境变量 `GEMINI_API_KEY`
4. 点击 Deploy

**特性：**
- 每次推送代码自动重新部署
- Pull Request 自动生成预览链接
- 自定义域名支持

---

### 3. Netlify

**特点：** 简单易用，支持表单处理

**设置步骤：**
1. 访问 [netlify.com](https://netlify.com) 并用 GitHub 账号登录
2. 选择 `seek-stars` 仓库
3. 构建设置：
   - Build command: `npm run build`
   - Publish directory: `dist`
4. 设置环境变量 `GEMINI_API_KEY`

---

## 🔧 环境变量配置

所有部署方案都需要设置 `GEMINI_API_KEY`：

1. 获取 API Key：
   - 访问 [Google AI Studio](https://makersuite.google.com/app/apikey)
   - 创建新的 API 密钥

2. 在部署平台设置环境变量：
   - GitHub Pages: Settings → Secrets → Actions
   - Vercel: Project Settings → Environment Variables
   - Netlify: Site settings → Environment variables

---

## 📋 部署检查清单

在部署前确保：

- [ ] 代码已推送到 GitHub 仓库
- [ ] `package.json` 包含正确的依赖
- [ ] 环境变量 `GEMINI_API_KEY` 已设置
- [ ] 构建命令 `npm run build` 本地运行成功
- [ ] 项目名称与仓库路径一致（如需要）

---

## 🛠 本地测试

在部署前测试本地构建：

```bash
npm install
npm run build
npm run preview
```

访问 `http://localhost:4173` 确认构建结果正常。

---

## 📞 遇到问题？

1. **构建失败**：检查 `npm run build` 输出的错误信息
2. **API 调用失败**：确认环境变量设置正确
3. **页面 404**：检查路由配置和 base path 设置
4. **资源加载失败**：检查静态资源路径是否正确

## 🔄 自动化流程

所有配置完成后，部署流程如下：

```
推送代码 → 触发 CI/CD → 自动构建 → 自动部署 → 更新上线
```

您只需专注于代码开发，部署完全自动化！